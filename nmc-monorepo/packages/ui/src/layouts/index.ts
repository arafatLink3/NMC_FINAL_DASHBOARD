/**
 * Barrel for `@nmc/ui/layouts`.
 *
 * Composite layouts that combine primitives and hooks into a full screen
 * structure (e.g. the top-bar + sidebar shell used by `apps/web` and
 * `apps/mobile`).
 */
export * from './AppShell.js';
