/**
 * Top-level entry for `@nmc/ui`.
 *
 * The package's `exports` map also exposes `.`, `./theme`, `./primitives`,
 * `./layouts`, `./hooks`, and `./platform` as separate subpaths, so apps
 * can import only what they need (e.g. `import { Button } from '@nmc/ui'`).
 */
export * from './theme/index.js';
export * from './platform.js';
export * from './hooks/index.js';
export * from './layouts/index.js';
export * from './primitives/index.js';
